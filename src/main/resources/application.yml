server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    url: jdbc:mariadb://dev-erp-rds.cztekutconfc.ap-northeast-2.rds.amazonaws.com/erp_db?autoReconnect=true
    username: root
    password: minsooco
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 500
#    open-in-view: false

  jackson:
    property-naming-strategy: SNAKE_CASE

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 935349671127-kie8p7ct045tph801rrde45a8t6f8cpc.apps.googleusercontent.com
            client-secret: 34Ifc6oTSWQ07HbrBInQf_oj
            scope:
              - email
              - profile

springdoc:
  swagger-ui:
    operations-sorter: alpha
    disable-swagger-default-url: true

logging.level:
  root: info
  org.hibernate.SQL: debug